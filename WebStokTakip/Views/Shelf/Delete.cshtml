@model WebStokTakip.Models.Shelf

@{
    ViewBag.Title = "Raf Sil";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container">
    @if (TempData["RafSilmeYetkisiYok"] != null)
    {
        <div class="alert alert-danger">
            <i class="fa-regular fa-circle-xmark"></i> @TempData["RafSilmeYetkisiYok"].ToString()
        </div>
    }
    <h4>Silmek istediğinize emin misiniz ?</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Warehouse.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Warehouse.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Capacity)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Capacity)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreatedDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IsActive)
        </dt>

        <dd>
            @if (Model.IsActive)
            {
                <span class="badge badge-success"><i class="fa-solid fa-check"></i> Evet</span>
            }
            else
            {
                <span class="badge badge-danger"><i class="fa-solid fa-xmark"></i> Hayır</span>
            }
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Sil" style="width:100px;" class="btn btn-danger" />
            <a href="/Shelf/Index" class="btn btn-warning">Raf Listesi</a>
        </div>
    }
</div>
